// Legacy file — no longer used. The webhook URL now lives server-side
// in Netlify's GHL_WEBHOOK_URL environment variable, set via the relay
// function (netlify/edge-functions/lead.mjs), not this file.
